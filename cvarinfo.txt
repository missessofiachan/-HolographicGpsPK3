// User settings saved to their main config file
user bool   holo_gps_enabled = true;      // Master switch
user int    holo_gps_spacing = 64;        // Distance between path markers
user int    holo_gps_freq = 5;            // Update frequency (in engine ticks)
user float  holo_gps_alpha = 0.6;         // Transparency/Glow intensity
user bool   holo_gps_use_secrets = false; // Route through secret doors
user int    holo_gps_max_markers = 12;    // Maximum number of arrows to draw
user bool   holo_gps_fade = true;         // Fade out distant arrows
user int    holo_gps_color = 0;           // Trail color: 0=Default, 1-8=Custom, 9=Trans Pride
user float  holo_gps_scale = 0.25;        // Size of the markers
user float  holo_gps_height = 1.0;        // Float height from the floor
user int    holo_gps_priority = 0;        // 0=Auto, 1=Keys Only, 2=Exits Only
user int    holo_gps_style = 0;           // 0=Additive, 1=Translucent
user bool   holo_gps_extended_search = true; // Find custom non-standard keys
user bool   holo_gps_wolfendoom_compat = false; // WolfenDoom compatibility mode

// New Pathfinding & Adaptive Learning Settings
user bool   holo_gps_hazard_avoidance = true;
user bool   holo_gps_target_snapping = true;
user bool   holo_gps_raycast_detouring = true;
user bool   holo_gps_learning_proven = true;
user bool   holo_gps_learning_pheromone = true;
user bool   holo_gps_learning_persistent = true;
user bool   holo_gps_learning_heuristics = true;
user string holo_gps_memory_blob = "";

// Speedrun Heuristics
user bool   holo_gps_sr_momentum = false;
user bool   holo_gps_sr_door_penalty = false;
user bool   holo_gps_sr_monster_avoidance = false;
user bool   holo_gps_sr_clearance = false;
user bool   holo_gps_sr_reckless = false;

// Advanced Engine Constants
user float  holo_gps_clearance_min = 56.0;
user float  holo_gps_step_max = 24.0;
user float  holo_gps_weight_doors = 500.0;
user float  holo_gps_weight_monsters = 80.0;
user float  holo_gps_weight_momentum = 150.0;

// Core Pathfinder Features
user bool   holo_gps_3dfloors = true;
user bool   holo_gps_portals = true;
user bool   holo_gps_solve_switches = true;
user bool   holo_gps_smoothing = true;

// Learning Datastore Limits
user int    holo_gps_learning_limit = 10;

